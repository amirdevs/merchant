# Step 14 Overlay - First Character Identity Catalog Batch

Direct root overlay. No patch scripts.

Adds the first full identity-catalog batch for useful new NPCs. This is still pre-portrait-generation.

Run after extracting:

```powershell
pnpm verify:current-state
pnpm build
```

Suggested commit message:

```bash
git commit -m "data: add first character identity catalog batch"
```
