const fs = require("fs");
const path = require("path");

const reportPath = path.join(process.cwd(), "docs", "logs", "runtime-loop-save-report.md");
fs.mkdirSync(path.dirname(reportPath), { recursive: true });

const report = `# Runtime Loop Save Report

Generated by \`pnpm playtest:runtime-loop\`.

## Scope

This report records the manual Phase 6 pass for moving the playable merchant loop from a standalone local-only state into the main GameState save/export model.

## Manual checks

1. Run \`pnpm test:runtime-loop\` and confirm the save-payload tests pass.
2. Run \`pnpm build\` and confirm the Journal still compiles.
3. Start a new game and open the Journal.
4. Confirm **Playable Merchant Loop v1** shows **Runtime: GameState**.
5. Confirm the runtime notice says the loop is stored under \`playableLoop\`.
6. Buy 4 Coastal Salt, travel, sell, resolve the first quest, and register the company through the loop panel.
7. Save the game from the system menu.
8. Reload/continue and confirm copper, day, cargo, loop ledger, quest-chain state, consequences, and company registration remain.
9. Export the save and inspect the JSON for the \`playableLoop\` field.
10. Reset the loop and confirm reset affects only the vertical-slice loop, not the broader generated character/item data.

## Acceptance criteria

- Runtime loop state is stored on GameState as \`playableLoop\`.
- Runtime loop actions no longer depend on the standalone \`merchant-playable-loop-v1\` local-storage slot when the Journal passes GameState.
- Main save/export includes loop day, cargo, profit, quest chain, trust/reputation/consequence flags, and company state.
- The standalone local-storage fallback remains only for isolated component tests.
- Phase 7 can expand economy/world/content without another persistence rewrite first.
`;

fs.writeFileSync(reportPath, report);
console.log(`Wrote ${reportPath}`);
console.log(report);
