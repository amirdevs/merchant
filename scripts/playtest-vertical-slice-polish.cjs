const fs = require("fs");
const path = require("path");

const reportPath = path.join(process.cwd(), "docs", "logs", "vertical-slice-polish-report.md");
fs.mkdirSync(path.dirname(reportPath), { recursive: true });

const report = `# Full Vertical Slice Polish Report

Generated by \`pnpm playtest:vertical-polish\`.

## Scope

Phase 8 turns the current saved merchant loop into a clean alpha-style vertical slice. It does not add another isolated system; it checks whether the existing trade, travel, rich quest, company, economy, save, and consequence layers feel like one playable game.

## Manual checks

1. Run \`pnpm verify:current-state\`.
2. Run \`pnpm build\`.
3. Open the Journal and confirm **Full Vertical Slice Polish** appears under the economy/world panel.
4. Start from Sunwake Harbor and buy a low-risk good.
5. Travel to Riverwake Mill and sell for profit.
6. Complete the five rich starter quests without treating the quest card like a simple todo list.
7. Confirm at least one consequence is visible through public trust, shadow heat, NPC trust, town reputation, or named flags.
8. Travel to Brasskeep Gate and register the first company.
9. Save, reload, and confirm town, day, copper, cargo, rich quest state, consequences, and company state remain.
10. Confirm the Journal gives a clear next action without developer notes.
11. Confirm debug/reset/demo controls are not the main path a normal player would use.
12. Record unclear UI text before any more towns, items, or quests are expanded.

## Acceptance criteria

- Build passes.
- Current-state verification passes.
- The player can understand the loop: buy, travel, sell, quest, company, save, continue.
- The alpha-readiness panel does not hide blockers; it surfaces them as visible checks.
- Remaining old-system panels are clearly marked as legacy or migration scaffolding.
- No new content is added unless it supports trade, story, company progression, or visible consequences.

## Next expansion rule

After this phase, avoid foundation churn. The next work should expand only when the vertical slice is understandable, persistent, and fun enough to play end-to-end.
`;

fs.writeFileSync(reportPath, report);
console.log(`Wrote ${reportPath}`);
console.log(report);
