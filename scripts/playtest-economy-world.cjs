const fs = require("fs");
const path = require("path");

const reportPath = path.join(process.cwd(), "docs", "logs", "economy-world-expansion-report.md");
fs.mkdirSync(path.dirname(reportPath), { recursive: true });

const report = `# Economy / World Expansion Report

Generated by \`pnpm playtest:economy-world\`.

## Scope

Phase 7 expands the saved playable merchant loop into a richer economy/world/content foundation.

## Manual checks

1. Run \`pnpm test:economy-world\`.
2. Run \`pnpm verify:current-state\`.
3. Run \`pnpm build\`.
4. Open the Journal.
5. Confirm **Playable Merchant Loop v1** still works from GameState.
6. Confirm **Economy / World Expansion** appears under the loop panel.
7. Confirm current-town stock rows show item names, stock pressure, dynamic prices, and readable story pressure.
8. Confirm route risk cards react after shadow/heat consequences are created by quest choices.
9. Complete/register the first company and confirm upgrade statuses become available/active.
10. Confirm the next town and quest seed lists are visible but do not yet pretend to be fully implemented towns.

## Acceptance criteria

- The expansion layer reads from the saved playableLoop state.
- The UI shows stock pressure, route risk, tuned items, company upgrades, and quest seeds.
- No new content is random filler: every expansion item supports merchant gameplay, company growth, world identity, or rich quests.
- The next phase can polish the full vertical slice instead of rewriting save/runtime again.
`;

fs.writeFileSync(reportPath, report);
console.log(`Wrote ${reportPath}`);
console.log(report);
